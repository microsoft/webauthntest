{
    "name": "webauthntest",
    "description": "Passkey Playground",
    "license": "MIT",
    "author": "Akshay Kumar",
    "version": "2026.01.16",
    "scripts": {
        "build": "esbuild src/client-backend.js --bundle --format=esm --platform=browser --target=es2022 --outfile=public/client-backend.js",
        "dev:worker": "npm run build && wrangler dev",
        "deploy:worker": "npm run build && wrangler deploy"
    },
    "dependencies": {
        "@noble/post-quantum": "^0.6.1",
        "cbor-x": "^1.6.0"
    },
    "devDependencies": {
        "esbuild": "^0.28.1",
        "wrangler": "^4.115.0"
    },
    "engines": {
        "node": ">=24.11.0"
    }
}
